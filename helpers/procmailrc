# se check if all the paths in PATH are reachable, remove the ones that
# are not.

PATH=$HOME:/bin:/usr/bin:/bin:/usr/local/bin:/usr/X11R6/bin:/home/oplog/bin:.
VERBOSE=on
MAILDIR=$HOME/mail        # make sure it exists
DEFAULT=$HOME/mail/mbox
PMDIR=$HOME/.mailproc
LOGFILE=$PMDIR/log
#LOGFILE=/dev/null
LOCKFILE=$PMDIR/.lockmail
SHELL=/bin/bash

:0fw
| /opt/oplog/bin/oplog_mailparser.py
