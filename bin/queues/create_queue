#!/usr/bin/env python
import sys

errMsg = 'Usage: ' + sys.argv[0] + ' "DB_name" "DB Title"'
testres = len(sys.argv)
if testres < 3:
	print errMsg
	sys.exit(1)


sys.path.append('/opt/oplog/conf')
import oplog
import opdb
opdb = opdb.opdb()
opdb.createqueue(sys.argv[1],sys.argv[2])
opdb.close()
